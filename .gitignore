# 依赖目录由 npm install 生成，不进入 git。
node_modules/
# electron-vite 构建产物。
out/
dist/
# electron-builder 发布产物。
release/
# 测试覆盖率输出。
coverage/
# 项目计划和规格文档由本地 OpenCode 流程维护，不纳入 git 提交。
docs/
.omo/
# 系统、日志和临时文件。
.DS_Store
*.log
*.tmp
